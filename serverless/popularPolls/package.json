{
    "scripts": {
      "test": "jest",
      "build": "tsc",
      "deploy": "npm run build && ./deploy.sh"
    },
    "devDependencies": {
      "@types/aws-lambda": "^8.10.0",
      "@types/jest": "^29.0.0",
      "@types/node": "^20.0.0",
      "jest": "^29.0.0",
      "ts-jest": "^29.0.0",
      "typescript": "^5.0.0"
    },
    "dependencies": {
      "@aws-sdk/client-s3": "^3.0.0"
    }
  }